{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Items</h1>
    <ul>
        {% for chose in choses %}
            <li><a href="/item/show/{{ chose.id }}">{{ chose.id }}. {{ chose.title }}</a></li>
        {% else %}
            <li>Nothing to display</li>
        {% endfor %}
    </ul>
    <a href="/item/add">Ajouter</a>
{% endblock %}
